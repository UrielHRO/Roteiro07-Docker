version: '3.8'
services:
  app:
    image: ubuntu:latest
    command: sleep infinity
    networks:
      secure_internal_network:
        aliases:
          - secure-app
networks:
  secure_internal_network:
    external: true